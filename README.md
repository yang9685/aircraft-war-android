# Aircraft War Android

Android migration workspace for the Aircraft War course project.

Current status:
- Android project skeleton created
- Main menu with difficulty selection and sound toggle added
- `SurfaceView` game page and `GameActivity` flow added
- Desktop image and audio assets imported
- Single-player game engine migrated with enemies, bullets, collisions, props, score, and boss phase
- Local leaderboard backed by `SharedPreferences` added
- GitHub Actions builds a debug APK artifact on every push
